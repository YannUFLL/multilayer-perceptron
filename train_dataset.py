import numpy 

class Layers:

    class DenseLayer:
        def __init__(self, layer_size, activation="sigmoid", weigth_initializer="heUniform"):
            init_value = 2 / 
            self.weight = np.random.uniform()
            self.neuron =  
            self.activation = activation
            self.weight_initializer = weigth_initializer


class Model: 

    def
